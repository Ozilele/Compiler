PROGRAM IS
    x, y, a, b, c
IN
    READ a;
    READ b;
    x:=a;
    y:=b;
    WHILE y>0 DO
        IF x>=y THEN 
            x:=x-y;
        ELSE 
            x:=x+y;
            y:=x-y;
            x:=x-y;    
        ENDIF
    ENDWHILE
    c:=x;
    WRITE c;
END